<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>OauthExample · Phoenix Framework</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body>
      <header class="navigation">
        <section class="container split_header">
          <ul class="navigation__list">
            <li class="navigation__item">DWA Fitness</li>
            <li class="navigation__item"><a href="<%= Routes.page_path(@conn, :index) %>">Home</a></li>
            <%= if @conn.assigns[:user] do%>
            <li class="navigation__item"><%= link "Workouts", to: Routes.workout_path(@conn, :index)%></li>
            <% end %>
          </ul>
          <div class="split-header_signout">
            <%= if @conn.assigns[:user] do%>
              <%= link "Logout", to: Routes.auth_path(@conn, :signout), class: "navigation__item" %>
            <% else %>
            
             <%= link "Github Signin", to: Routes.auth_path(@conn, :request, "github"), class: "navigation__item"%></li>
             <%= link "Discord Signin", to: Routes.auth_path(@conn, :request, "discord"), class: "navigation__item" %></li>
              
            <% end %>
          </div>
        </section>
      </header>  
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= @inner_content %>
    </main>
  </body>
</html>
