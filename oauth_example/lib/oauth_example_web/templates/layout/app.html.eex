<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>OauthExample · Phoenix Framework</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body>
    <nav class="header">
      <header>
        <div class="container">
        <div class="row">
        <div class="column">
          <strong>DWA Learning</strong>
        </div>
        <div class="column-offset-25">
        <%= if @conn.assigns[:user] do%>
          <%= link "Logout", to: Routes.auth_path(@conn, :signout), class: "button" %>
        <% else %>
        
          <%= link "Sign in with github", to: Routes.auth_path(@conn, :request, "github"), class: "button" %>
          <%= link "Sign in with discord", to: Routes.auth_path(@conn, :request, "discord"), class: "button" %>         
        <% end %>
        </div>
        </row>
      </div>
    </header>
    </nav>
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= @inner_content %>
    </main>
  </body>
</html>
